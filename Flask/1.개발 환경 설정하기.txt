1. groomide 설치가 필요 없는 통합 개발 사이트 컨테이너를 생성

2. 공개 범위는 작업하는 동안에 공유를 원하면 Public 아닐 경우 Private로 진행

3. 소프트 웨어 스택을 Flask로 선택

4. 생성

5. 실행

6. 컨테이너 상에 플라스트의 기본 구조가 띄어짐 (if __name__ == "__main__": application.run(host='0.0.0.0') 기존 포트 값 제거

7. 기본 세팅으로 프로젝트 -> URL과 포트를 설정 -> URL 이름 작성하고 보통 포트를 5000을 사용하니 선택함/ 기존 80 포트 삭제

8. URL로 들어가 빈 페이지를 확인해 볼 수 있음

9. 컨테이너 폴더로 만들었던 Build 하위에 templetes를 만들어줌

10. @application.route("/")의 슬래시는 웹 사이트에서 검색을 하게 될 때 하위에 추가 주소 만들어지면서 다른 페이지를 출력해줌

11. 현재는 하위에 아무것도 작성하지 않았으므로 기본 페이지라고 볼 수 있음

12. from flask import Flask, render_template

13. ,render_template을 추가로 작성해줌

14. @application.route("/")
	def hello():
    		return render_template("hello.html") templetes 하위에 html을 만들어 str으로 render_template에 작성

15 터미널에 python3 application.py를 작성

16. 기존 URL을 눌러보면 hello.html로 접속됨을 확인 할 수 있음