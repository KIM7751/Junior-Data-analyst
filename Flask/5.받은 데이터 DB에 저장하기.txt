1. 여러가지 데이터들을 데이터베이스에 저장을 하고 사진이랑 데이터를 연결할 것임

2. database.py, database.csv는 제작자 링크 참조

3. database.py // database.py 모듈을 통해서 데이터를 저장하고 데이터를 불러옴

4. database.csv // pandas 모듈을 이용

	@application.route("/applyphoto")
	def photo_apply():
    		location = request.args.get("location")
    		cleaness = request.args.get("clean")
    		built_in = request.args.get("built")
    		if cleaness == None:
        		cleaness = False
    		else:
        		cleaness = True
        
    		database.save(location, cleaness, built_in)
    		return render_template("apply_photo.html")

5. 입력 정보를 바탕으로 database 모듈의 save 함수를 이용해 정보 저장하는 구조



	@application.route("/upload_done", methods = ["POST"])
	def upload_done():
    		uploaded_files = request.files["file"]
    		uploaded_files.save("static/img/{}.jpeg".format(database.now_index()))
    		return redirect(url_for("hello"))

6. 입력된 정보와 업로드한 사진을 매칭 시키기위해 database 모듈의 now_index()를 활용해 라벨링함
 


